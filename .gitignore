.env
# Local config override (config.py loads it after .env) — do not commit; can force DRY_RUN=true in deploys
/env
funding_farmer.log
trades.csv
trades_dry_run.csv
trades_dry_run.csv.*_backup
trades.csv.header_backup
__pycache__/
*.pyc
farmer_cycle.jsonl
claude_advisor_out.jsonl
.claude_advisor_last_run
.alert_watcher_state.json
reviews/
.code_review_last_run

# Local-only artifacts from Fly log analysis
fly-logs/
reports/

# Cursor local rules / editor metadata
.cursor/
